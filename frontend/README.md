# Frontend

קובץ יחיד: **`index.html`** – טקסטarea, כפתורי "Find Similar Cases" ו-"Full RAG Analysis", כרטיסי תוצאות וניתוח RAG.

**הרצה:** להפעיל את ה-backend (מקומי או Docker על פורט 8000), ואז לפתוח את `index.html` בדפדפן. הקובץ יפנה אוטומטית ל-`http://localhost:8000`.
