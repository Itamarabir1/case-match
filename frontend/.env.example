# Frontend only. Copy to frontend/.env when you add a build (e.g. Vite, Next.js).
# Static HTML today – API URL is in index.html. When you have a build:
# VITE_API_URL=http://localhost:8000
# or NEXT_PUBLIC_API_URL=http://localhost:8000
