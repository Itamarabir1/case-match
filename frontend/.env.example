# Frontend – static HTML (no server).
# API base URL is set in index.html (http://localhost:8000 when opening from file).
# If you add a build step later, you can use:
# VITE_API_URL=http://localhost:8000
# or NEXT_PUBLIC_API_URL=http://localhost:8000
